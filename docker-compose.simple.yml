version: '3.8'

services:
  # EmbyTok 前端应用 - 简单版配置
  embytok:
    image: ghcr.io/1525745393/embytok:latest
    container_name: embytok-web
    restart: unless-stopped
    ports:
      - "8080:80"  # Web界面端口
    environment:
      - NODE_ENV=production
    network_mode: bridge
networks: {}